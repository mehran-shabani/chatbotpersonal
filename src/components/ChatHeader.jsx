import React, { useState } from 'react'; import { Typography, Box, IconButton, Tooltip, Menu, MenuItem, Divider } from '@mui/material'; import { GiHamburgerMenu } from 'react-icons/gi'; import { FiSun, FiMoon } from 'react-icons/fi'; const ChatHeader = ({ isDarkMode, toggleDarkMode, models, selectedModelIndex, setSelectedModelIndex }) => { const [anchorEl, setAnchorEl] = useState(null); const handleMenuClick = (event) => { setAnchorEl(event.currentTarget); }; const handleMenuClose = () => { setAnchorEl(null); }; const handleModelSelect = (index) => { setSelectedModelIndex(index); handleMenuClose(); }; return ( <> <Box display="flex" justifyContent="space-between" alignItems="center" mb={3}> <Typography variant="h4" fontWeight="bold" color="primary">Chat Demo</Typography> <Box display="flex" alignItems="center"> <Tooltip title="Select AI Model"> <IconButton onClick={handleMenuClick} color="primary"> <GiHamburgerMenu /> </IconButton> </Tooltip> <Tooltip title={isDarkMode ? "Switch to Light Mode" : "Switch to Dark Mode"}> <IconButton onClick={toggleDarkMode} color="primary"> {isDarkMode ? <FiSun /> : <FiMoon />} </IconButton> </Tooltip> </Box> </Box> <Divider sx={{ mb: 2 }} /> <Menu anchorEl={anchorEl} open={Boolean(anchorEl)} onClose={handleMenuClose} > {models.map((model, index) => ( <MenuItem key={index} onClick={() => handleModelSelect(index)}> {model.description || model.model} </MenuItem> ))} </Menu> </> ); }; export default ChatHeader;